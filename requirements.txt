# requirements.txt — chỉ liệt kê thư viện thực sự được import trong mã nguồn.
#
# Quy ước phiên bản: chặn CẬN DƯỚI (>=) để có tính năng cần dùng, và chặn CẬN TRÊN
# ở mốc major kế tiếp (<) để một bản phát hành có breaking change không tự động
# làm sập ứng dụng. Ghim cứng "==" toàn bộ (kết quả của pip freeze) khiến máy khác
# và Streamlit Cloud không giải được phụ thuộc; thả lỏng hoàn toàn thì môi trường
# không tái lập được. Khoảng phiên bản là điểm cân bằng giữa hai thái cực đó.

streamlit>=1.49,<2.0        # cần >=1.49 cho st.plotly_chart(width="stretch")
pandas>=2.2,<4.0
numpy>=1.26,<3.0
scipy>=1.11,<2.0            # scipy.optimize.minimize — đường biên hiệu quả
statsmodels>=0.14,<0.16     # hồi quy OLS có t-stat/p-value cho CAPM & APT; VIF
plotly>=5.20,<7.0
requests>=2.31,<3.0         # gọi endpoint entrade
yfinance>=0.2.40,<1.0       # báo cáo tài chính
groq>=0.11,<1.0             # trợ lý AI (trang 8)
